{
  "name": "@studio/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "smoke": "npm --prefix ../../packages/shared run build && npm run build && node ./scripts/import-smoke.mjs && node ./scripts/export-smoke.mjs"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/formbody": "^8.0.2",
    "@fastify/multipart": "^9.0.3",
    "@studio/shared": "*",
    "exceljs": "^4.4.0",
    "fast-xml-parser": "^5.7.1",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
